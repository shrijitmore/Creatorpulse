node_modules
dist

# ── Secrets — never commit these ──────────────────────────────────────────────
.env
.env.*
*.env
*.env.local
*.env.production
*.env.staging
**/.env
**/.env.*
# Service account JSON if ever saved as a standalone file
*service_account*.json
*serviceaccount*.json
# Cloud Run env-vars files (real values) — keep only the *.example.yaml template
deploy/env.prod.yaml
deploy/env.*.yaml
**/deploy/env.prod.yaml
!**/deploy/env.*.example.yaml

# Frontend
frontend/node_modules
frontend/dist

# Backend
backend/node_modules
backend/dev.log
*.log

# OS
.DS_Store
Thumbs.db
Thumbs.db:encryptable

# IDE
.vscode/settings.json
.idea/

# Claude Code — personal/local config only (commit shared commands/agents if added)
.claude/settings.local.json
.claude/*.local.json
